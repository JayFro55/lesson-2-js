const firstname = 'Jason ';
const lastname = 'Xie ';
const age = 23;
const x = 16;

if (age >= x) {
  console.log(firstname + lastname + 'can drive and has driven for ' + (age - x) + ' years.');
} else {
  for (let i = 5; i>=0; i--) {
    console.log(i);
  }
}